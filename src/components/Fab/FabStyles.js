import {StyleSheet} from 'react-native';
import {Colors} from '../../theme';

export default StyleSheet.create({
  container: {
    width: 60,
    height: 51,
  },
  plusSign: {
    width: '100%',
    resizeMode: 'cover',
  },
});
