import BottomAlertSnackBarController from './BottomAlertSnackBarController';
import {connect} from 'react-redux';

const BottomAlert = connect()(BottomAlertSnackBarController);

export default BottomAlert;
